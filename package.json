{
  "name": "cf-flickr-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Edge proxy (route allowlist / CORS / status pass-through) in front of ippoan/rust-flickr (Cloud Run).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.67.0"
  }
}
